import { StyleSheet } from 'react-native';

// Выносим стили в отдельный файл для лучшей структуризации
// В будущем здесь могут быть дополнительные стили
export const styles = StyleSheet.create({
    // Можно добавить дополнительные стили здесь
    // если они понадобятся в других компонентах
});